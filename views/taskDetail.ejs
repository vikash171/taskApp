<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= task.title %></title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: black;
            color: white;
        }
        .container {
            margin: 50px auto;
            width: 80%;
            max-width: 600px;
            background-color: #222;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
        }
        h1 {
            color: #5cb85c;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1><%= task.title %></h1>
        <p><%= task.description %></p>
        <a href="/" style="color: #5cb85c;">Back to tasks</a>
    </div>
</body>
</html>
