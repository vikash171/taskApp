<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Task Manager</title> 

    <style>
        body {
            background-color: black;
            color: white;
            font-family: Arial, sans-serif;
        }
        .save {
            margin: 20px auto;
            width: 90%;
            max-width: 1200px;
            background-color: #222;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
        }
        textarea, input {
            background-color: blueviolet;
            color: white;
            border: none;
            width: 100%;
            height: 40px;
            margin-bottom: 15px;
            padding: 10px;
            border-radius: 4px;
            resize: none;
        }
        button {
            display: inline-block;
            padding: 10px 20px;
            background-color: #5cb85c;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }
        button:hover {
            background-color: #4cae4c;
        }
        .box {
            margin: 10px 0;
            border: 2px solid red;
            width: 200px;
            display: flex;
            flex-direction: column;
            align-items: center;
            position: relative;
            padding: 10px;
        }
        h3 {
            color: red;
        }
        .description {
            max-height: 50px;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
        }
        a {
            color: blue;
            position: absolute;
            bottom: 10px;
            left: 10px;
        }
    </style>
</head>
<body>
    <div class="save">
         <form action="/add-task" method="POST">
            <input type="text" name="title" placeholder="Task title..." required>
            <textarea name="description" placeholder="Task description..." required></textarea>
            <button type="submit">Submit</button>
         </form>

        <% if (files.length > 0 && files) { %>
            <% files.forEach(function(value) { %>
                <div class="box">
                    <h1><%= value.tittle %></h1>
                    <p class="description"><%= value.description.substring(0, 50) %></p>
                    <a href="/task/<%= value.id %>">Read More</a>
                </div>
            <% }); %>
        <% } else { %>
            <div>
                <h3>No tasks...</h3>
            </div>
        <% } %>
    </div>  
</body>
</html>
